{
  "name": "@stack/config",
  "version": "0.0.0",
  "private": true,
  "description": "Typed environment: one Zod schema + getEnv() (parse once, cached). The 'never hardcode, always getEnv()' door.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14"
  },
  "nx": {
    "tags": [
      "type:lib"
    ]
  }
}
